# Playwright drives the actual browser session. Installing this package is
# not enough on its own - it also needs a browser binary, which pip/uv cannot
# fetch for you. After installing, run:
#   playwright install chromium
# See README's Setup section.
playwright>=1.40

# Patches the browser fingerprint signals Walmart's bot detection checks for
# (WebGL vendor, navigator.webdriver, iframe.contentWindow, media codecs,
# and more). Load-bearing, not optional - confirmed live that Walmart walls
# off requests without it. Never pass its *_override kwargs a hardcoded
# value (UA, WebGL renderer, etc.) - see walmart_mcp_server.py's page().
playwright-stealth>=2.0
